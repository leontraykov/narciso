<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Narciso Perez Fine Art</title>
    <!-- mobile responsive meta -->
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
  
    <!-- theme meta -->
    <meta name="theme-name" content="galaxy" />

    <!-- ** Plugins Needed for the Project ** -->
    <!-- Подключите здесь Bootstrap, Main Stylesheet и другие необходимые файлы стилей -->

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <!--Favicon-->
    <link rel="shortcut icon" href="<%= asset_path 'favicon.png' %>" type="image/x-icon">
    <link rel="icon" href="<%= asset_path 'favicon.png' %>" type="image/x-icon">
</head>
<body>
    <!-- Предположим, что у вас есть partial header -->
    <section class="d-flex">

    <%= render partial: 'partials/sidenav', locals: {home: 'active', about: '', contact: '', pages: '', home2: '', category: '', postDetails: '', pnp: ''} %>

    
    <!-- Здесь будет выводиться основное содержимое -->
    <%= yield %>
    
    </section>
</body>
</html>
